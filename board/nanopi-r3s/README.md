# Board Support — NanoPi R3S (RK3566)

Contains device tree and board-specific definitions for the NanoPi R3S
running the kei kernel.

## Files

- `device-tree/rk3566-nanopi-r3s.dts` — Boot device tree
- `config.toml` — Board build parameters (TBD, or use configs/nanopi-r3s.toml)
