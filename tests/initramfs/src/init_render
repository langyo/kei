#!/bin/sh
mount -t proc none /proc 2>/dev/null
mount -t sysfs none /sys 2>/dev/null
echo ""
echo "=== kei render test (aarch64) ==="
echo "Running aris-render render_test on /dev/fb0..."
/render_test /tmp/render.ppm
echo "render_test exited with $?"
echo "Done. Sleeping..."
while true; do sleep 10; done
